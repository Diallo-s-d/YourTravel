// const filterTravels = (price) => {
//   const filteredTravel = travels.filter((travel) => {
//     const priceFilter =
//       (price === "9999" && travel.price <= 10000) ||
//       (price === "19999" && travel.price > 10000 && travel.price <= 20000) ||
//       (price === "29999" && travel.price > 20000 && travel.price <= 30000) ||
//       (price === "30000" && travel.price > 30000) ||
//       price === "";

//     return priceFilter;
//   });

//   return filteredTravel;
// };

// export default filterTravel;
